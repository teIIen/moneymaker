# TODO и план по Moneymaker2

## 1) Миграция на MT5 сервер
- [x] Добавлен URL MT5 сервера в `config/settings.py`.
- [x] `core_lib/data_provider.py` переведен на получение OHLCV через MT5 HTTP API.
- [x] Проверена совместимость формата свечей с `VSAStrategyEngine` (`open/high/low/close/volume`).
- [x] Добавлен проверочный скрипт `scripts/check_mt5_server.py`.
- [x] Удалены неактуальные зависимости `ccxt` и `tiingo` из `requirements.txt`.
- [x] Удалены неиспользуемые legacy-провайдеры (`crypto_data_provider.py`, `forex_data_provider.py`).

## 2) Адаптация стратегии под M30
- [ ] Переключить рабочий `TIMEFRAME` на `30m` в `config/settings.py` после согласования.
- [ ] Провести 1-2 недели paper-forward на M30 с текущими порогами:
  - `is_climax: vol_ratio > 2.0`
  - близость к уровню: `0.5%`
- [ ] По результатам откалибровать пороги под M30:
  - `vol_ratio` (ориентир: 1.6-2.0),
  - порог близости к уровням (ориентир: 0.3%-0.5%).
- [ ] Утвердить целевой `POLL_INTERVAL_SECONDS` для M30 (ориентир: 60-300 секунд).

## 3) Новости и сентимент
- [x] Полностью удален текущий контур новостей (`NewsAnalyzer`, зависимости `nltk/tiingo`).
- [ ] При подключении новой площадки новостей добавить отдельный адаптер-источник без изменений ядра стратегии.

## 4) Экспорт аналитики для сайта
- [ ] Добавить отдельный API/хранилище для выдачи последних сигналов и метрик (не только Telegram).
- [ ] Зафиксировать контракт данных для фронта: тренд, уровни, VSA-метрики, риск-план, timestamp.
